eureka:
  instance:
    prefer-ip-address: true

spring:
  application:
    name: microservice-product-client
  cloud:
    config:
      enabled: true
      label: main
      uri: http://localhost:7000
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379